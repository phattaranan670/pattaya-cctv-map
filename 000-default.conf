<VirtualHost *:80>
    DocumentRoot /var/www/html
    
    # 🌟 Reverse Proxy: ถ้ามี request มาที่ /yolo-detect/... 
    # ให้ส่งไปที่ Python Server ที่รันบน http://localhost:5000/
    ProxyPass /yolo-detect/ http://localhost:5000/
    ProxyPassReverse /yolo-detect/ http://localhost:5000/
    
    # Apache Config สำหรับเสิร์ฟไฟล์ Frontend/PHP
    <Directory /var/www/html>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
